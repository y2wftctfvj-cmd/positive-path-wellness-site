# Positive Path Wellness — Static Site Bundle

## Run locally
- Open `index.html` directly, or (recommended) run a local server.

### Quick local server (Python 3)
From this folder:
python3 -m http.server 8080

Then open:
http://localhost:8080

## Deploy
- Netlify Drop: drag this folder into Netlify.
- GitHub Pages: put these files in a repo and enable Pages.

## Form
Replace `maqqnprd` in:
- /index.html (priority list)
- /providers/index.html (provider interest)
